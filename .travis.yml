sudo: false

language: php
php:
    - 5.3
    - 5.4
    - 5.5
    - 5.6
    - hhvm

before_install:
    - nvm install 6

install:
    - npm install -g yarn
    - yarn global add bower
    #- "npm config set spin false"
    #- npm install -g bower
    - yarn install

script:
    - npm run build
    - ".travis-build/phplint ."
    - phpunit

notifications:
