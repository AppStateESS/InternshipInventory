sudo: false

language: php
php:
    - 5.3
    - 5.4
    - 5.5
    - 5.6
    - hhvm

before_install:
    - nvm 4

install:
    #- npm install -g yarn
    #- yarn
    - "npm config set spin false"
    - npm install -g bower
    - npm install
    - node -v

script:
    - npm run build
    - ".travis-build/phplint ."
    - phpunit

notifications:
